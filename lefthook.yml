pre-push:
  jobs:
    - name: packages audit
      tags:
        - frontend
        - security
      run: pnpm audit

pre-commit:
  parallel: true
  jobs:
    - run: biome format --write {staged_files}
      glob: "*.{js,ts,jsx,tsx}"

    - run: taplo format {staged_files}
      glob: "*.toml"
